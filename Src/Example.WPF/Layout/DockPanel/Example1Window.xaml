<UserControl x:Class="Example.WPF.Layout.DockPanel.Example1Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example.WPF.Layout.DockPanel"
        mc:Ignorable="d">
    <!--
        DockPanel:
            可以讓子控制項依照指定方向 (Dock) 排列的容器，
            如果同時設定了 DockPanel.Dock 屬性，則會依照加入的順序排列，
            如果沒有設定 DockPanel.Dock 屬性，則會使用預設值 Left
    -->
    <DockPanel>
        <!--Dock 預設設定為 Left，代表子控制項會靠左排列-->
        <Button Content="DefaultLeft" />
        <!--Dock 設定為 Right，代表子控制項會靠右排列-->
        <Button Content="Right" DockPanel.Dock="Right" />
        <!--Dock 設定為 Top，代表子控制項會靠上排列-->
        <Button Content="Top" DockPanel.Dock="Top" />
        <!--Dock 設定為 Bottom，代表子控制項會靠下排列-->
        <Button Content="Bottom" DockPanel.Dock="Bottom" />
        <Button Content="Auto" />
    </DockPanel>
</UserControl>
